# Auth

Supabase Auth actions and server-side guards for MarketFlow.

- `auth.ts` contains Server Actions for login, registration, and logout.
- `guards.ts` contains Server Component helpers for route protection and auth-page redirects.
