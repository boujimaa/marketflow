# Meta

Meta OAuth helpers shared by server routes and services.

This folder builds authorization URLs and exchanges Facebook OAuth codes for
long-lived tokens. It does not publish content or fetch Pages/Instagram assets.
